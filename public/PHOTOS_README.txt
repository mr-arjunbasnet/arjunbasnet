PHOTOS — DROP THESE TWO FILES HERE
====================================

The site expects two profile photos. If they're missing,
the site automatically falls back to a styled "AB" placeholder.

1. arjun-hero.jpg   — used on the Home page hero (right column)
                     → recommended: the COLOR photo on yellow background

2. arjun-about.jpg  — used on the About page header (right column)
                     → recommended: the BLACK & WHITE photo on blue background

Recommended specs:
  • Square aspect ratio (1:1)
  • Minimum 800 × 800 px
  • JPG or PNG (just rename to match the filenames above)
  • File size under 500 KB for fast loading

Just drag-and-drop your two photos into this /public/ folder
with the filenames above. No code changes needed.

Delete this file after you add the photos.
